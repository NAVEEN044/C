#include<stdio.h>
#include<conio.h>
int main()
{
char str[20];
int i,count=0;
printf("enter the string");
scanf("S",&str[i]);
for(i=0;str[i]!=null;i++)
{
if(str[i]>=1||str[i]<=9)
{
count++;
}
printf("%d",count);
}
